apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/kiendo96/banking-gitops.git'
    targetRevision: main
    path: postgres/kustomize/overlays/dev
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dev-infra
  syncPolicy:
    # automated:
    #   prune: true
    syncOptions:
      - CreateNamespace=true
