apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-vault
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/kiendo96/banking-gitops.git"
    targetRevision: HEAD
    path: kustomize/base/generated/vault
  destination:
    server: "https://kubernetes.default.svc"
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
