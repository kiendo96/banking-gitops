apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-external-secrets
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/kiendo96/banking-gitops.git"
    targetRevision: HEAD
    path: kustomize/base/generated/external-secrets
  destination:
    server: "https://kubernetes.default.svc"
    namespace: external-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
