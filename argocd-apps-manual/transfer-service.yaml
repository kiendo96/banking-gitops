apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-transfer-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/kiendo96/banking-gitops.git"
    targetRevision: HEAD
    path: kustomize/base/generated/transfer-service
  destination:
    server: "https://kubernetes.default.svc"
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
